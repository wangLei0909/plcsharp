<UserControl x:Class="PLCSharp.VVMs.Authority.UserManage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PLCSharp.VVMs.Authority"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" d:Background="White"
             d:DataContext="{d:DesignInstance Type=local:UserManageViewModel}"
             Width="640" Height="360">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid>

            <DataGrid Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Grid}, AncestorLevel=1}, Path=ActualWidth}"
                      Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Grid}, AncestorLevel=1}, Path=ActualHeight}"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      AutoGenerateColumns="False" CanUserAddRows="False"
                      CanUserDeleteRows="False" CanUserSortColumns="False"
                      ItemsSource="{Binding Model.UserList}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      SelectedItem="{Binding Model.SelectUser}"
                      Unloaded="DataGrid_Unloaded">
                <DataGrid.ContextMenu>
                    <ContextMenu IsEnabled="True" StaysOpen="False">
                        <MenuItem Command="{Binding Model.Manage}" CommandParameter="PasswordChange" Header="修改密码" />
                        <MenuItem Command="{Binding Model.Manage}" CommandParameter="Delete" Header="删除" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn MinWidth="200" Binding="{Binding Name}" Header="用户" IsReadOnly="True" />
                    <DataGridTextColumn MinWidth="200" Binding="{Binding Authority}" Header="权限" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Width="50" Margin="5"
                    Command="{Binding Model.Manage}"
                    CommandParameter="Register" Content="注册" />
        </StackPanel>

    </Grid>
</UserControl>